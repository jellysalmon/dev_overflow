<h1><%= @question.title %></h1>
<h3><%= @question.content %></h3>
<p>
  <%= link_to("Delete", [@question],
                        method: :delete,
                        data: {confirm: "Are you sure?"}) %>

  <%= link_to("Edit", edit_question_path) %>
</p>

<h3> Post an Answer</h3>
<%= render "answer_form" %>

<ul> 
  <li> <%= render @question.answers %> </li>
</ul>


<%= link_to("Back", questions_path) %>
